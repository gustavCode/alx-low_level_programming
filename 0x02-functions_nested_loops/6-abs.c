#include "main.h"

/**
 * _abs - computes the abolute value of an integer
 * @a: the integer to be checked
 *
 * Return: absolute value of n
 */
int _abs(int a)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}
