#include "main.h"
#include <stdio.h>

/**
 * main - Print the programs name, followed by newline
 * @argc: count of arguments
 * @argv: pointer to and array of strings
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
